# Runtime dependencies, pinned to exact versions.
#
# Pinned rather than ranged because an earlier revision used `mcp>=1.0.0`, which
# silently resolved to a 2.x release that had moved a module, and a clean
# `pip install -r requirements.txt` stopped importing entirely. Reproducible
# builds beat automatic upgrades for anything anyone else has to run.

# --- Web ---
fastapi==0.141.1
uvicorn[standard]==0.52.0
python-multipart==0.0.32
streamlit==1.60.0

# --- Configuration ---
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- LLM and orchestration ---
openai==2.51.0
langchain-core==1.5.3
langchain-openai==1.4.1
langchain-text-splitters==1.1.2
langgraph==1.2.10

# --- Vector store ---
lancedb==0.36.0
pyarrow==24.0.0
pandas==3.0.5

# --- Document parsing ---
PyMuPDF==1.28.0
python-docx==1.2.0
